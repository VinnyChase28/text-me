export const welcomeMessage = (apiName: string, occurrence: string) => {
  return `Welcome to ${apiName} Bot! You will receive ${occurrence} updates. Text STOP WEATHER any time to stop. Or, use STOP ALL to stop all alerts.`;
};
